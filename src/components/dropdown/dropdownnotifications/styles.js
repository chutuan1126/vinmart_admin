import { colors } from 'styles/theme';

export const styles = theme => ({
  root: {
    position: 'relative'
  },
  notiButton: {
    marginLeft: 0
  },
  notiBadge: {
    backgroundColor: colors.orange
  },
});