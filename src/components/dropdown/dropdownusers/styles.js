export const styles = theme => ({
  root: {
    position: 'relative'
  },
  avatarButton: {
    padding: 0,
    marginLeft: theme.spacing(1)
  },
  avatar: {
    width: 32,
    height: 32,
    fontSize: 16
  }
});